ext {
    gemfireVersion = '9.4.0'
}

apply plugin: 'java'
apply plugin: 'application'

repositories {
    mavenCentral()
    maven { url 'https://repo.spring.io/libs-release' }
}

dependencies {
    compile "io.pivotal.gemfire:geode-core:$gemfireVersion"
}
